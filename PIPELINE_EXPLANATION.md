# 🔬 GhostPen ML Pipeline — Детальное объяснение

## 🎯 Суть системы

**GhostPen НЕ просто отправляет тему в LLM.**

GhostPen делает **трёхэтапный процесс**:

1. **ML извлекает особенности** из постов автора → создаёт **стилевой профиль**
2. **Профиль превращается в промпт** с ограничениями и примерами
3. **LLM генерирует** с учётом всех ограничений стиля

---

## 📊 Полный Pipeline

### **ЭТАП 1: Подготовка данных (один раз)**

```
dataset.json (посты авторов)
    ↓
[Style Profiler]
    ↓
author_profiles.json (стилевые профили)
```

**Что делает Style Profiler:**

Для каждого автора анализирует **все его посты** и извлекает:

#### Метрики стиля:
```python
# Длина
avg_post_length = mean([len(post) for post in posts])  # Например: 300 символов

# Структура
uses_lists = count(posts with numbered/bullet lists)  # True/False
structure_type = detect_structure(posts)  # "paragraphs" или "numbered_lists"

# Тон (через keyword matching)
formal_score = count(formal_words) / total_words * 1000  # Например: 13.39
emotional_score = count(emotional_words) / total_words * 1000  # Например: 4.46
dominant_tone = max(formal, emotional, expert, casual)  # "formal"

# Эмодзи и хэштеги
emoji_density = total_emojis / total_posts  # Например: 0.2 на пост
hashtag_density = total_hashtags / total_posts  # Например: 2.2 на пост

# Характерные фразы (N-grams)
signature_phrases = extract_frequent_phrases(posts)  # ["тот кто", "мы перестаём"]
```

**Результат:** Профиль автора с **числовыми метриками** и **примерами**

---

### **ЭТАП 2: Генерация поста (каждый раз)**

```
Запрос: author_id, platform, topic
    ↓
[Prompt Builder]
    ↓
Детальный промпт (500-1000 токенов)
    ↓
[LLM (OpenAI)]
    ↓
Сырой текст
    ↓
[Post-Processing]
    ↓
Готовый пост
```

---

## 🔍 Детально: Что передаётся в LLM?

### **1. Стилевые характеристики (из профиля):**

```
Тон: формальный, профессиональный
Длина поста: примерно 305 символов
Структура: структурированные абзацы
Эмоциональность: умеренная
Эмодзи: редко использует (0.2 на пост в среднем)
Хэштеги: использует активно (2.2 на пост в среднем)
Характерные фразы автора: тот кто, мы перестаём, перестаём расти
```

### **2. Примеры постов автора (Few-shot learning):**

```
ПРИМЕРЫ ПОСТОВ ЭТОГО АВТОРА:

Пример 1:
Вчера на конференции услышал фразу, которая засела в голове: 
'Успех — это не финальная точка, а процесс постоянного роста'.

Действительно, когда мы перестаём учиться, мы перестаём расти...

Пример 2:
Обратная связь — это подарок. Но только если она конструктивная...

Пример 3:
Недавно провёл ретроспективу с командой и заметил интересную закономерность...
```

**Зачем:** LLM видит **реальные примеры** стиля автора и учится их имитировать.

### **3. Правила платформы:**

```
ТРЕБОВАНИЯ ПЛАТФОРМЫ:

Длина: 300-800 символов
Тон: профессиональный, экспертный
Структура: структурированные абзацы, можно использовать списки
Эмодзи: минимум эмодзи (0-1 на пост)
Хэштеги: 2-5 хэштегов, профессиональные
Общий стиль: формальный, но доступный
```

### **4. Тема поста:**

```
ТЕМА ПОСТА: О важности планирования
```

### **5. Требования к формату:**

```
ТРЕБОВАНИЯ К ФОРМАТУ:
- Пост должен быть на русском языке
- Сохраняй структуру (абзацы, переносы строк)
- Можно использовать нумерованные или маркированные списки
- Не добавляй лишних символов или форматирования
- Пост должен звучать естественно и человечно
```

---

## 🤖 Что делает LLM?

LLM получает **весь этот промпт** и:

1. **Анализирует примеры** постов автора
2. **Понимает стиль** из метрик (формальный, списки, длина)
3. **Применяет правила** платформы
4. **Генерирует текст** на заданную тему
5. **Имитирует стиль** автора на основе примеров

**Это как дать LLM:**
- "Вот как пишет этот автор" (примеры)
- "Вот его стиль" (метрики)
- "Напиши так же, но про эту тему"

---

## 🎨 Почему это работает лучше, чем просто промпт?

### **Обычный подход:**
```
Промпт: "Напиши пост про планирование в формальном стиле"
LLM: [генерирует универсальный формальный текст]
```

### **GhostPen подход:**
```
Промпт: 
  - Стиль автора: формальный, 305 символов, использует списки
  - Примеры: [3 реальных поста автора]
  - Характерные фразы: "тот кто", "мы перестаём"
  - Платформа: LinkedIn (формальный, 2-5 хэштегов)
  - Тема: планирование
  
LLM: [генерирует текст, который звучит как автор]
```

**Разница:** LLM видит **конкретные примеры** и **метрики**, а не просто описание.

---

## 📈 Пример работы Pipeline

### Вход:
```json
{
  "author_id": "person_01",
  "social_network": "linkedin",
  "topic": "О важности планирования"
}
```

### Шаг 1: Загрузка профиля
```python
profile = {
  "style": {
    "avg_post_length": 300,
    "tone": {"dominant": "formal"},
    "uses_lists": true,
    "emoji_density": 0.2
  },
  "signature_phrases": ["тот кто", "мы перестаём"],
  "sample_posts": ["Вчера на конференции...", "Обратная связь..."]
}
```

### Шаг 2: Построение промпта
```python
prompt = """
Ты пишешь пост в стиле автора person_01 для платформы LINKEDIN 
на тему "О важности планирования".

СТИЛЬ АВТОРА:
Тон: формальный, профессиональный
Длина: примерно 305 символов
Структура: структурированные абзацы
Использует списки: да
Характерные фразы: тот кто, мы перестаём

ПРИМЕРЫ ПОСТОВ:
[3 примера постов автора]

ТРЕБОВАНИЯ ПЛАТФОРМЫ:
Длина: 300-800 символов
Тон: профессиональный, экспертный
Эмодзи: минимум (0-1)
Хэштеги: 2-5

ТЕМА: О важности планирования
"""
```

### Шаг 3: Генерация через LLM
```python
response = openai.ChatCompletion.create(
    model="gpt-3.5-turbo",
    messages=[
        {"role": "system", "content": "Ты эксперт по созданию контента..."},
        {"role": "user", "content": prompt}
    ]
)
# LLM генерирует текст на основе промпта
```

### Шаг 4: Post-processing
```python
# Корректирует длину, убирает повторы, форматирует
processed_text = post_processor.process(
    raw_text,
    target_length=305,
    emoji_density=0.2,
    structure_type="paragraphs"
)
```

### Шаг 5: Оценка
```python
similarity = scorer.score(processed_text, profile, "linkedin")
# Возвращает: 0.77 (77% соответствие стилю)
```

### Выход:
```json
{
  "generated_post": "Сегодня хочу поделиться мыслями о важности планирования...",
  "style_similarity": 0.77
}
```

---

## 🔑 Ключевые моменты

1. **ML не генерирует текст** — ML **анализирует** и **извлекает особенности**
2. **LLM генерирует** — но с **детальными ограничениями** из профиля
3. **Post-processing корректирует** — под метрики автора
4. **Scorer оценивает** — насколько получилось похоже

---

## 💡 Аналогия

Представь, что ты хочешь, чтобы кто-то написал текст "как ты":

1. **GhostPen анализирует** твои тексты → находит паттерны (длина, тон, фразы)
2. **GhostPen показывает** примеры твоих текстов → "вот как ты пишешь"
3. **GhostPen даёт задание** → "напиши так же, но про эту тему"
4. **LLM пишет** → имитируя твой стиль
5. **GhostPen проверяет** → "похоже ли на тебя?"

---

## ✅ Итог

**GhostPen = ML Analysis (извлечение стиля) + Prompt Engineering (ограничения) + LLM (генерация) + Post-processing (корректировка)**

Это **контролируемая генерация стиля**, а не просто "LLM с промптом".

