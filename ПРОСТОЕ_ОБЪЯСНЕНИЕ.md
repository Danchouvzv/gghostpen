# 🧠 GhostPen — Как это работает (простыми словами)

## 🎯 Короткий ответ

**Да, именно так!** GhostPen:

1. **Анализирует посты персоны** → извлекает особенности стиля
2. **Передаёт эти особенности в LLM (OpenAI)** → как ограничения и примеры
3. **LLM генерирует пост** → учитывая стиль автора и тему

---

## 📊 Визуальная схема

```
┌─────────────────────────────────────────────────────────────┐
│  ЭТАП 1: ПОДГОТОВКА (один раз)                              │
└─────────────────────────────────────────────────────────────┘

dataset.json (посты авторов)
         ↓
    [Style Profiler]
    Анализирует посты:
    • Длина, структура
    • Тон (формальный/эмоциональный)
    • Эмодзи, хэштеги
    • Характерные фразы
    • Примеры постов
         ↓
author_profiles.json (стилевой профиль)


┌─────────────────────────────────────────────────────────────┐
│  ЭТАП 2: ГЕНЕРАЦИЯ (каждый запрос)                         │
└─────────────────────────────────────────────────────────────┘

Запрос: author_id, platform, topic
         ↓
    [Prompt Builder]
    Строит промпт:
    • Стиль автора (из профиля)
    • Примеры постов автора
    • Правила платформы
    • Тема поста
         ↓
    Детальный промпт (500-1000 токенов)
         ↓
    [LLM (OpenAI)]
    Генерирует текст:
    • Учитывает стиль из примеров
    • Следует ограничениям
    • Пишет на заданную тему
         ↓
    Сырой текст
         ↓
    [Post-Processing]
    Корректирует:
    • Длину
    • Структуру
    • Эмодзи
         ↓
    Готовый пост
```

---

## 🔍 Что именно извлекается из постов?

### **1. Статистические метрики:**

```python
# Из всех постов автора вычисляется:
avg_post_length = 300  # Средняя длина
avg_sentence_length = 10.18  # Средняя длина предложений
uses_lists = True  # Использует ли списки
emoji_density = 0.2  # Сколько эмодзи на пост
hashtag_density = 2.2  # Сколько хэштегов на пост
```

### **2. Тон (через анализ слов):**

```python
# Подсчитываются маркеры тона:
formal_words = ["применяю", "рекомендую", "ключевой"]  # Найдено: 13.39
emotional_words = ["чувствую", "вдохновляет"]  # Найдено: 4.46
expert_words = ["анализ", "стратегия"]  # Найдено: 4.46

# Доминирующий тон: "formal"
```

### **3. Характерные фразы:**

```python
# Извлекаются часто встречающиеся фразы (2-3 слова):
signature_phrases = [
    "тот кто",      # Встречается 5 раз
    "мы перестаём", # Встречается 4 раза
    "перестаём расти"  # Встречается 3 раза
]
```

### **4. Примеры постов:**

```python
# Сохраняются реальные посты автора:
sample_posts = [
    "Вчера на конференции услышал фразу...",
    "Обратная связь — это подарок...",
    "Недавно провёл ретроспективу..."
]
```

---

## 🤖 Что передаётся в LLM (OpenAI)?

### **Полный промпт выглядит так:**

```
Ты пишешь пост в стиле автора person_01 для платформы LINKEDIN 
на тему "О важности планирования".

СТИЛЬ АВТОРА:
Тон: формальный, профессиональный
Длина поста: примерно 305 символов
Структура: структурированные абзацы
Эмодзи: редко использует (0.2 на пост)
Хэштеги: использует активно (2.2 на пост)
Характерные фразы автора: тот кто, мы перестаём

ПРИМЕРЫ ПОСТОВ ЭТОГО АВТОРА:

Пример 1:
Вчера на конференции услышал фразу, которая засела в голове: 
'Успех — это не финальная точка, а процесс постоянного роста'.

Действительно, когда мы перестаём учиться, мы перестаём расти...

Пример 2:
Обратная связь — это подарок. Но только если она конструктивная...

Пример 3:
Недавно провёл ретроспективу с командой и заметил интересную закономерность...

ТРЕБОВАНИЯ ПЛАТФОРМЫ:
Длина: 300-800 символов
Тон: профессиональный, экспертный
Эмодзи: минимум (0-1 на пост)
Хэштеги: 2-5 хэштегов

ТЕМА ПОСТА: О важности планирования

ТРЕБОВАНИЯ К ФОРМАТУ:
- Сохраняй структуру (абзацы, переносы)
- Можно использовать списки
- Пост должен звучать естественно
```

---

## 💡 Как LLM использует это?

1. **Видит примеры** → понимает, "как пишет этот автор"
2. **Видит метрики** → понимает ограничения (длина, тон)
3. **Видит тему** → знает, о чём писать
4. **Генерирует** → имитируя стиль из примеров + следуя ограничениям

**Это как:**
- Показать LLM: "Вот как пишет этот человек" (примеры)
- Сказать: "Напиши так же, но про эту тему"
- Дать ограничения: "Длина 300 символов, формальный тон, можно списки"

---

## ✅ Итог

**Да, система работает именно так:**

1. ✅ **ML извлекает особенности** из постов персоны
2. ✅ **Передаёт их в LLM** как ограничения и примеры
3. ✅ **LLM генерирует** пост в стиле автора

**Это не просто "LLM с промптом"**, а **контролируемая генерация стиля** с метриками, примерами и ограничениями.

