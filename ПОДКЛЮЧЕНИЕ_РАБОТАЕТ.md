# ✅ Подключение Frontend ↔ Backend

## 📊 Текущая конфигурация

- **Frontend**: http://localhost:3000 ✅
- **Backend**: http://localhost:8000 ✅
- **API Base URL**: `http://localhost:8000` (в `services/api.ts`)

## ✅ Что работает правильно

1. **Backend корневой URL** (`http://localhost:8000/`):
   - Возвращает JSON с информацией о сервисе - это **нормально** ✅
   - Это правильное поведение FastAPI

2. **API Endpoints**:
   - `/api/health` - проверка здоровья
   - `/api/authors` - список авторов
   - `/api/generate` - генерация постов

3. **CORS настроен**:
   - `allow_origins=["*"]` - разрешает запросы с любого источника
   - Это позволяет фронтенду на порту 3000 обращаться к бэкенду на порту 8000

## 🧪 Проверка в браузере

### 1. Откройте http://localhost:3000

### 2. Откройте DevTools (F12) → Console

### 3. Выполните тест:

```javascript
// Тест 1: Health check
fetch('http://localhost:8000/api/health')
  .then(r => r.json())
  .then(data => console.log('✅ Backend работает!', data))
  .catch(err => console.error('❌ Ошибка:', err));

// Тест 2: Получить авторов
fetch('http://localhost:8000/api/authors')
  .then(r => r.json())
  .then(data => console.log('✅ Авторы:', data))
  .catch(err => console.error('❌ Ошибка:', err));
```

### 4. Попробуйте сгенерировать пост:

1. Выберите автора (например, "Alex Rivera" = `person_01`)
2. Выберите платформу (LinkedIn)
3. Введите тему
4. Нажмите "Generate"
5. Проверьте Network tab - должен быть запрос к `http://localhost:8000/api/generate`

## 🔍 Что проверить в Network tab

После нажатия "Generate":

1. Найдите запрос к `http://localhost:8000/api/generate`
2. Проверьте:
   - **Status**: `200 OK` ✅
   - **Request Headers**: `Origin: http://localhost:3000`
   - **Response Headers**: `Access-Control-Allow-Origin: *`
   - **Response**: JSON с `generated_post`

## ✅ Всё должно работать!

Если видите:
- ✅ Запросы проходят (Status 200)
- ✅ Нет ошибок CORS в консоли
- ✅ Пост генерируется и отображается

**Значит Frontend и Backend подключены правильно!** 🎉

## 🐛 Если есть проблемы

### CORS ошибка:
```
Access to fetch at 'http://localhost:8000/api/generate' from origin 'http://localhost:3000' has been blocked by CORS policy
```

**Решение**: Убедитесь, что в `api/main.py`:
```python
allow_origins=["*"]  # Должно быть так
```

### 404 Not Found:
**Решение**: Проверьте URL в `services/api.ts`:
```typescript
const API_BASE_URL = 'http://localhost:8000';  // Должно быть так
```

### Backend не отвечает:
**Решение**: Проверьте, что backend запущен:
```bash
curl http://localhost:8000/api/health
```

