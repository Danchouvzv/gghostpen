# 🚀 Деплой GhostPen на Vercel

## ✅ Что нужно сделать перед деплоем

### 1. Исправить ошибки
- ✅ Убедиться что проект собирается: `npm run build`
- ✅ Проверить что нет дублирующихся ключей в `locales/en.ts` и `locales/ru.ts`

### 2. Настроить переменные окружения в Vercel

После деплоя фронтенда, в настройках Vercel проекта добавьте:

**Environment Variables:**
```
VITE_API_URL=https://your-backend-url.com
```

Где `your-backend-url.com` - это URL вашего задеплоенного backend API.

### 3. Backend должен быть задеплоен отдельно

**Варианты деплоя backend:**
- **Railway** (https://railway.app) - простой деплой Python/FastAPI
- **Render** (https://render.com) - бесплатный tier доступен
- **Fly.io** (https://fly.io) - хорошая производительность
- **Heroku** (https://heroku.com) - классический вариант

**Важно:** Backend должен быть доступен по HTTPS и иметь CORS настроенный для вашего Vercel домена.

### 4. Деплой фронтенда

1. **Подключите GitHub репозиторий к Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "New Project"
   - Выберите репозиторий `Danchouvzv/gghostpen`
   - Укажите **Root Directory**: `frontend`

2. **Настройки проекта:**
   - Framework Preset: **Vite**
   - Build Command: `npm run build` (автоматически)
   - Output Directory: `dist` (автоматически)
   - Install Command: `npm install` (автоматически)

3. **Environment Variables:**
   - Добавьте `VITE_API_URL` с URL вашего backend

4. **Деплой:**
   - Нажмите "Deploy"
   - Vercel автоматически соберет и задеплоит проект

### 5. После деплоя

1. Проверьте что сайт открывается
2. Проверьте что API запросы идут на правильный backend URL
3. Если есть CORS ошибки - обновите `allow_origins` в `api/main.py`

## ⚠️ Важные моменты

1. **Backend должен быть задеплоен ПЕРЕД фронтендом** - иначе API не будет работать
2. **CORS** - убедитесь что backend разрешает запросы с вашего Vercel домена
3. **Environment Variables** - не забудьте добавить `VITE_API_URL` в Vercel
4. **Database** - если используете SQLite, рассмотрите переход на PostgreSQL для продакшена

## 🔧 Troubleshooting

### Ошибка: "Failed to fetch"
- Проверьте что `VITE_API_URL` правильно настроен
- Проверьте что backend доступен и CORS настроен

### Ошибка: "Build failed"
- Проверьте что все зависимости установлены
- Проверьте что нет синтаксических ошибок в TypeScript

### Ошибка: "Module not found"
- Убедитесь что все импорты правильные
- Проверьте что `package.json` содержит все зависимости

## 📝 Пример конфигурации Vercel

Файл `vercel.json` уже создан и содержит правильную конфигурацию для Vite проекта.

