# 🔑 Настройка OpenAI API ключа

## ✅ Текущий статус

OpenAI API ключ **установлен и работает**! Backend использует реальную генерацию через OpenAI.

## 🚀 Быстрый старт

### Вариант 1: Установить в текущей сессии

```bash
export OPENAI_API_KEY="your-api-key-here"
cd /Users/danialtalgatov/Documents/ghostpen/api
python3 main.py
```

### Вариант 2: Добавить в ~/.zshrc (постоянно)

```bash
echo 'export OPENAI_API_KEY="your-api-key-here"' >> ~/.zshrc
source ~/.zshrc
```

### Вариант 3: Создать .env файл (рекомендуется)

```bash
cd /Users/danialtalgatov/Documents/ghostpen
echo 'OPENAI_API_KEY=your-api-key-here' > .env
```

Затем обновите `start_backend.sh` для загрузки `.env` (уже сделано).

## ✅ Проверка работы

### Тест 1: Health check

```bash
curl http://localhost:8000/api/health
```

### Тест 2: Генерация поста

```bash
curl -X POST http://localhost:8000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "author_id": "person_01",
    "social_network": "linkedin",
    "topic": "О важности командной работы"
  }'
```

Должен вернуться **реально сгенерированный пост** через OpenAI.

## 🔍 Как понять, что используется реальная генерация?

1. **В логах backend** не будет сообщений "⚠️ используется mock"
2. **Посты будут более разнообразными** и контекстуально релевантными
3. **Время генерации** будет ~1-3 секунды (OpenAI API)

## ⚠️ Безопасность

**НЕ коммитьте API ключ в Git!**

Добавьте в `.gitignore`:
```
.env
*.key
```

## 📊 Текущая конфигурация

- ✅ OpenAI API ключ установлен
- ✅ Backend читает ключ из `OPENAI_API_KEY`
- ✅ Используется новый формат OpenAI API (v1)
- ✅ Mock генерация используется только при ошибках

## 🎯 Готово!

Теперь GhostPen использует **реальную генерацию через OpenAI GPT**!

