# ✅ Проверка подключения Frontend ↔ Backend

## 📊 Текущая конфигурация

### ✅ Что настроено правильно:

1. **Frontend** (`services/api.ts`):
   - API URL: `http://localhost:8000` ✅
   - Использует реальный API (не mock) ✅
   - Endpoint: `/api/generate` ✅

2. **Backend** (`api/main.py`):
   - CORS: `allow_origins=["*"]` ✅
   - Порт: `8000` ✅
   - Endpoints работают ✅

## 🧪 Быстрая проверка

### В браузере (http://localhost:3000):

1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Console**
3. Выполните:

```javascript
fetch('http://localhost:8000/api/health')
  .then(r => r.json())
  .then(data => {
    console.log('✅ Backend подключен!', data);
  })
  .catch(err => {
    console.error('❌ Ошибка:', err);
  });
```

### Ожидаемый результат:

✅ **Если работает**: увидите `{status: "healthy", ...}`

❌ **Если ошибка CORS**: 
- Проверьте, что backend запущен
- Проверьте настройки CORS в `api/main.py`

## 🔍 Проверка в интерфейсе

1. Откройте http://localhost:3000
2. Выберите автора (например, "Alex Rivera")
3. Выберите платформу (например, LinkedIn)
4. Введите тему (например, "О важности планирования")
5. Нажмите **"Generate"**
6. Откройте **DevTools → Network**
7. Найдите запрос к `http://localhost:8000/api/generate`
8. Проверьте:
   - Статус: `200 OK` ✅
   - Response: должен содержать `generated_post` ✅

## 🐛 Возможные проблемы

### Проблема: "Failed to fetch" или CORS error

**Решение**: 
1. Убедитесь, что backend запущен: `curl http://localhost:8000/api/health`
2. Проверьте CORS в `api/main.py` (должно быть `allow_origins=["*"]`)

### Проблема: 404 Not Found

**Решение**: Проверьте URL в `services/api.ts`:
```typescript
const API_BASE_URL = 'http://localhost:8000';
```

### Проблема: Пост не генерируется

**Решение**: 
1. Проверьте консоль браузера на ошибки
2. Проверьте Network tab - какой статус у запроса?
3. Проверьте, что `author_id` существует (например, `person_01`)

## ✅ Чеклист

- [ ] Backend запущен: http://localhost:8000
- [ ] Frontend запущен: http://localhost:3000
- [ ] В консоли браузера нет ошибок CORS
- [ ] Запрос к `/api/generate` возвращает 200
- [ ] Пост генерируется и отображается

## 🎯 Готово!

Если все проверки пройдены - **Frontend и Backend работают вместе!** 🎉

