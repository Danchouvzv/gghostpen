<div align="center">

# 🧠 GhostPen

> **Пишет как ты. Звучит как ты. Это ты.**

[![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![React](https://img.shields.io/badge/React-19-blue.svg)](https://react.dev/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**AI-система для генерации постов в уникальном авторском стиле**

[🚀 Быстрый старт](#-быстрый-старт) • [📖 Документация](#-документация) • [🏗️ Архитектура](#️-архитектура) • [💡 Примеры](#-примеры)

---

</div>

## 🎯 Что это?

**GhostPen** — это не просто генератор текстов. Это **система имитации авторского стиля**, которая:

- 📊 **Анализирует** твои посты и извлекает уникальный стиль
- 🎨 **Генерирует** новые посты, которые звучат как ты
- 🌐 **Адаптирует** стиль под разные платформы (LinkedIn, Instagram, Telegram, Facebook)
- 📈 **Оценивает** качество генерации через метрики стилевого сходства

### Простыми словами:

> Ты даёшь свои посты → GhostPen изучает твой стиль → GhostPen пишет новые посты, которые звучат как ты

**Без fine-tuning. Без сложных моделей. Просто умный анализ + LLM.**

---

## ✨ Ключевые особенности

### 🎯 **Стиль-ориентированная генерация**
- Извлечение **142+ параметров стиля** из твоих постов
- Анализ тона, эмоциональности, структуры, характерных фраз
- Контекстный анализ (не просто ключевые слова)

### 🌐 **Платформо-специфичная адаптация**
- **LinkedIn** → формально, структурированно, минимум эмодзи
- **Instagram** → эмоционально, с эмодзи, короче
- **Telegram** → свободно, размышления, длиннее
- **Facebook** → универсально, сбалансированно

### 📊 **Количественные метрики**
- **Style Similarity** (0-1) — насколько похоже на тебя
- **Length Accuracy** — соответствует ли длина
- **Structural Match** — совпадает ли структура
- **Tone Match** — соответствует ли тон

### ⚡ **Производительность**
- Кэширование профилей и промптов
- Оптимизированная обработка текста
- Быстрая генерация (1-3 сек)

### 🔒 **Персональные профили**
- Каждый пользователь может создать свой стилевой профиль
- Загрузка своих постов через API
- Автоматический анализ стиля

---

## 🏗️ Архитектура

```
┌─────────────────┐
│   User Posts    │
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Style Profiler  │ ← Анализ стиля (142+ параметров)
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Prompt Builder  │ ← Построение промпта с ограничениями
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  LLM (OpenAI)   │ ← Генерация текста
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│ Post Processor  │ ← Корректировка под метрики
└────────┬─────────┘
         │
         ▼
┌─────────────────┐
│  Style Scorer   │ ← Оценка качества
└────────┬─────────┘
         │
         ▼
   Готовый пост
```

### Компоненты системы:

#### 1. **Style Profiler** (`scripts/style_profiler.py`)
Анализирует посты и извлекает:
- 📏 Длину постов (средняя, мин, макс)
- 🎨 Структуру (абзацы, списки, narrative)
- 💬 Характерные фразы (биграммы, триграммы)
- 🎭 Тон (формальный, эмоциональный, экспертный, casual)
- 😊 Эмоциональность (0-10)
- 📱 Плотность эмодзи и хэштегов
- 📊 Тематику (career, motivation, personal, expertise, business)

#### 2. **Prompt Builder** (`scripts/prompt_builder.py`)
Строит детальный промпт для LLM:
- Стилевые характеристики автора
- Примеры постов автора
- Правила платформы
- Тема поста
- Требования к формату

#### 3. **GhostPen Generator** (`scripts/ghostpen_generator.py`)
Оркестрирует генерацию:
- Использует Prompt Builder для создания промпта
- Взаимодействует с LLM (OpenAI или mock)
- Применяет Post Processor для финальной обработки

#### 4. **Post Processor** (`scripts/ghostpen_generator.py`)
Обрабатывает сгенерированный текст:
- Корректирует длину
- Убирает повторы
- Настраивает эмодзи и хэштеги
- Форматирует структуру

#### 5. **Style Scorer** (`scripts/style_scorer.py`)
Оценивает качество генерации:
- Style Similarity (косинусное сходство)
- Length Accuracy
- Emoji/Hashtag Density Match
- Structure Match
- Tone Match

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.9+
- Node.js 18+ (для фронтенда)
- OpenAI API ключ (опционально, для реальной генерации)

### 1. Клонирование репозитория

```bash
git clone https://github.com/Danchouvzv/gghostpen.git
cd gghostpen
```

### 2. Установка зависимостей

#### Backend:
```bash
# Установка Python зависимостей
pip install -r api/requirements.txt
pip install -r scripts/requirements.txt
```

#### Frontend:
```bash
cd frontend
npm install
```

### 3. Настройка OpenAI API (опционально)

Создайте файл `api/.env`:
```bash
OPENAI_API_KEY=sk-proj-your-key-here
```

> **Примечание:** Если API ключ не указан, система будет использовать mock генерацию для тестирования.

### 4. Подготовка данных

#### Для демо-авторов (опционально):
```bash
# Валидация датасета
python scripts/prepare_dataset.py validate dataset/dataset.json dataset/schema.json

# Генерация стилевых профилей
python scripts/style_profiler.py dataset/dataset.json dataset/author_profiles.json
```

> **Примечание:** Датасет опционален. Система работает и с персональными профилями пользователей из БД.

### 5. Запуск Backend

```bash
# Через скрипт (рекомендуется)
./start_backend.sh

# Или вручную
cd api
python main.py
```

Backend будет доступен на: `http://localhost:8000`
- API документация: `http://localhost:8000/docs`
- Health check: `http://localhost:8000/api/health`

### 6. Запуск Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен на: `http://localhost:5173` (или другой порт Vite)

---

## 📖 Документация

### API Endpoints

#### `GET /api/health`
Проверка здоровья сервиса.

**Ответ:**
```json
{
  "status": "healthy",
  "profiles_loaded": true,
  "profiles_path": "/path/to/author_profiles.json"
}
```

#### `GET /api/authors`
Получить список доступных авторов (демо + персональные профили).

**Параметры:**
- `user_id` (опционально) - ID пользователя для включения его профиля

**Ответ:**
```json
{
  "authors": [
    {
      "id": "person_01",
      "name": "Айдар Нұрғалиев",
      "profession": "CEO & Основатель",
      "avatar": "https://...",
      "platforms": ["linkedin", "instagram"],
      "sample_posts": [...],
      "stats": {
        "total_posts": 50,
        "platforms_count": 2,
        "formality": "formal",
        "avgLength": 450,
        "emojiDensity": "Low"
      },
      "is_demo": true
    }
  ]
}
```

#### `POST /api/generate`
Генерация поста в стиле автора.

**Запрос:**
```json
{
  "author_id": "person_01",  // или "user_id": "uuid"
  "social_network": "linkedin",
  "topic": "О важности командной работы"
}
```

**Ответ:**
```json
{
  "generated_post": "Текст сгенерированного поста...",
  "style_similarity": 0.77,
  "debug": {
    "target_length": 450,
    "model_version": "ghostpen-v1.1-enhanced",
    "processing_time_ms": 1234,
    "prompt_tokens": 850
  }
}
```

#### `POST /api/users`
Создать нового пользователя.

**Запрос:**
```json
{
  "email": "user@example.com",
  "name": "Иван Иванов"
}
```

#### `POST /api/users/{user_id}/posts`
Добавить пост пользователя.

**Запрос:**
```json
{
  "platform": "linkedin",
  "content": "Текст поста...",
  "hashtags": ["#leadership"],
  "emojis": ["🔥"],
  "mentions": []
}
```

#### `POST /api/users/{user_id}/rebuild-profile`
Перестроить стилевой профиль пользователя на основе его постов.

**Ответ:**
```json
{
  "status": "success",
  "profile": {...},
  "total_posts": 15
}
```

### Полная документация API

- **Swagger UI:** `http://localhost:8000/docs`
- **ReDoc:** `http://localhost:8000/redoc`
- **USER_API.md:** Детальная документация по пользовательским эндпоинтам

---

## 💡 Примеры использования

### Python API

```python
from scripts.ghostpen_generator import GhostPenGenerator
from pathlib import Path

# Инициализация генератора
generator = GhostPenGenerator(
    profiles_path=Path("dataset/author_profiles.json"),
    api_key="sk-proj-..."  # Опционально
)

# Генерация поста
result = generator.generate_post(
    author_id="person_01",
    platform="linkedin",
    topic="О важности планирования"
)

print(result['generated_post'])
print(f"Style Similarity: {result['metrics']['style_similarity']}")
```

### REST API (cURL)

```bash
# Генерация поста
curl -X POST "http://localhost:8000/api/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "author_id": "person_01",
    "social_network": "linkedin",
    "topic": "О важности планирования"
  }'
```

### JavaScript/TypeScript

```typescript
import { generatePost } from './services/api';

const result = await generatePost({
  author_id: 'person_01',
  social_network: 'linkedin',
  topic: 'О важности планирования'
});

console.log(result.generated_post);
console.log(`Style Similarity: ${result.style_similarity}`);
```

---

## 🎨 Особенности реализации

### ✨ Контролируемая генерация

GhostPen использует **не просто промпт, а извлечение метрик → constraints → генерация**:

1. **Извлечение метрик** из постов автора
2. **Преобразование в ограничения** для LLM
3. **Генерация с учетом ограничений**
4. **Post-processing** для точной подгонки

### 🧠 Few-shot Learning

LLM получает **примеры постов автора** в промпте, что позволяет:
- Учиться на примерах
- Понимать стиль через контекст
- Генерировать более точные тексты

### 📊 Метрики как Constraints

Длина, структура, эмодзи — это **ограничения**, а не просто описания:
- Post-processing корректирует результат под метрики
- LLM получает точные требования
- Результат соответствует стилю автора

### 🌐 Платформо-специфичная адаптация

Один автор может писать по-разному на разных платформах:
- **LinkedIn:** формально, структурированно
- **Instagram:** эмоционально, с эмодзи
- **Telegram:** свободно, размышления
- **Facebook:** универсально

GhostPen это учитывает через **платформо-специфичные правила**.

---

## 🔧 Технологии

### Backend
- **Python 3.9+** — основной язык
- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM для БД
- **OpenAI API** — LLM для генерации
- **python-dotenv** — управление переменными окружения

### ML Pipeline
- **NLP анализ** — извлечение стилевых метрик
- **Статистика** — расчет средних, медиан, частот
- **Регулярные выражения** — обработка текста
- **Кэширование** — оптимизация производительности

### Frontend
- **React 19** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик
- **Tailwind CSS** — стилизация
- **Lucide React** — иконки

### Database
- **SQLite** — для MVP (легко мигрировать на PostgreSQL)

---

## 📊 Результаты и метрики

### Текущие показатели:
- ✅ **10 демо-авторов** в датасете
- ✅ **50+ постов** на автора
- ✅ **4 платформы** (LinkedIn, Instagram, Facebook, Telegram)
- ✅ **Style Similarity: 0.6-0.8** (хорошее сходство)
- ✅ **Время генерации: 1-3 сек** (с OpenAI API)
- ✅ **Производительность: +50%** (с кэшированием)

### Улучшения (v1.1):
- ✅ Контекстный анализ тона
- ✅ Улучшенное извлечение signature phrases
- ✅ Кэширование профилей и промптов
- ✅ Оптимизация работы с временными файлами
- ✅ Валидация входных данных
- ✅ Детальная обработка ошибок

---

## 🛠️ Разработка

### Структура проекта

```
ghostpen/
├── 📁 api/                    # FastAPI Backend
│   ├── main.py               # API эндпоинты
│   ├── database.py           # Работа с БД
│   ├── requirements.txt      # Python зависимости
│   └── .env                  # Переменные окружения
│
├── 📁 scripts/               # ML Pipeline
│   ├── style_profiler.py     # Анализ стиля
│   ├── style_profiler_enhanced.py  # Улучшенная версия
│   ├── prompt_builder.py     # Построение промптов
│   ├── ghostpen_generator.py # Генератор постов
│   ├── style_scorer.py       # Оценка качества
│   └── prepare_dataset.py    # Валидация датасета
│
├── 📁 dataset/               # Данные (опционально)
│   ├── dataset.json          # Основной датасет
│   ├── author_profiles.json  # Стилевые профили
│   └── schema.json           # JSON Schema
│
├── 📁 frontend/              # React Frontend
│   ├── components/           # React компоненты
│   ├── services/             # API клиент
│   ├── contexts/             # React Context
│   ├── locales/              # Переводы (RU/EN)
│   └── package.json          # NPM зависимости
│
├── 📄 README.md              # Этот файл
├── 📄 HOW_IT_WORKS.md        # Детальное описание ML pipeline
├── 📄 IMPROVEMENTS.md        # Список улучшений
├── 📄 QUICKSTART.md          # Быстрый старт
└── 📄 INTEGRATION.md         # Интеграция Backend + Frontend
```

### Запуск в режиме разработки

#### Backend:
```bash
cd api
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend:
```bash
cd frontend
npm run dev
```

### Тестирование

```bash
# Валидация датасета
python scripts/prepare_dataset.py validate dataset/dataset.json dataset/schema.json

# Статистика датасета
python scripts/prepare_dataset.py stats dataset/dataset.json

# Генерация профилей
python scripts/style_profiler.py dataset/dataset.json dataset/author_profiles.json

# Тест генерации
python scripts/ghostpen_generator.py
```

---

## 🚀 Деплой

### Backend

**Рекомендуемые платформы:**
- **Railway** (https://railway.app) — простой деплой
- **Render** (https://render.com) — бесплатный tier
- **Fly.io** (https://fly.io) — хорошая производительность

**Требования:**
- Python 3.9+
- Переменные окружения: `OPENAI_API_KEY`
- База данных: SQLite (для MVP) или PostgreSQL (для продакшена)

### Frontend

**Vercel** (https://vercel.com) — рекомендуемый вариант

**Настройка:**
1. Подключите GitHub репозиторий
2. Укажите Root Directory: `frontend`
3. Добавьте переменную окружения: `VITE_API_URL=https://your-backend-url.com`
4. Deploy!

Подробные инструкции: `frontend/VERCEL_DEPLOY.md`

---

## 📈 Roadmap

### ✅ Реализовано (v1.1)
- [x] Базовый ML pipeline
- [x] Style Profiler
- [x] Prompt Builder
- [x] Post Generator
- [x] Style Scorer
- [x] REST API
- [x] Frontend (React)
- [x] Персональные профили пользователей
- [x] Кэширование и оптимизация
- [x] Валидация и обработка ошибок
- [x] Локализация (RU/EN)

### 🔄 В планах
- [ ] Rate limiting (базовый реализован)
- [ ] Метрики и мониторинг (Prometheus)
- [ ] Unit и integration тесты
- [ ] Миграция на PostgreSQL
- [ ] WebSocket для real-time генерации
- [ ] Batch генерация постов
- [ ] Экспорт постов в разные форматы

---

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

### Как внести вклад:
1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'feat: add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект создан для хакатона. Используйте свободно.

---

## 👥 Авторы

**GhostPen Team** — Сделано с ❤️ для хакатона

---

## 🔗 Полезные ссылки

- **GitHub:** https://github.com/Danchouvzv/gghostpen
- **API Docs:** http://localhost:8000/docs (после запуска)
- **Документация:**
  - [HOW_IT_WORKS.md](HOW_IT_WORKS.md) — детальное описание ML pipeline
  - [INTEGRATION.md](INTEGRATION.md) — интеграция Backend + Frontend
  - [QUICKSTART.md](QUICKSTART.md) — быстрый старт
  - [IMPROVEMENTS.md](IMPROVEMENTS.md) — список улучшений

---

<div align="center">

**Сделано с ❤️ для хакатона**

[⬆ Наверх](#-ghostpen)

</div>
