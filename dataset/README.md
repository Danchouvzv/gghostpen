# 📊 GhostPen Dataset

Датасет для обучения и тестирования модели GhostPen — системы генерации постов в авторском стиле.

## 📁 Структура данных

Датасет представлен в формате JSON со следующей структурой:

```json
{
  "version": "1.0",
  "generated_at": "2025-02-12T10:00:00Z",
  "authors": [
    {
      "author_id": "person_01",
      "platforms": {
        "linkedin": [...],
        "instagram": [...],
        "facebook": [...],
        "telegram": [...]
      }
    }
  ]
}
```

## 📋 Формат поста

Каждый пост содержит:

- **`post_id`** (обязательно) — уникальный идентификатор поста
- **`content`** (обязательно) — текст поста (минимум 20 символов)
- **`timestamp`** (опционально) — дата публикации в формате ISO 8601
- **`meta`** (опционально) — метаданные:
  - `hashtags` — массив хэштегов
  - `mentions` — массив упоминаний
  - `emojis` — массив эмодзи

### Пример поста

```json
{
  "post_id": "ln_001",
  "content": "Сегодня хочу поделиться мыслями о лидерстве. За последние 5 лет я понял, что настоящий лидер — это не тот, кто командует, а тот, кто вдохновляет команду на достижение общих целей.",
  "timestamp": "2024-08-10T12:22:15Z",
  "meta": {
    "hashtags": ["#leadership", "#management"],
    "mentions": [],
    "emojis": ["💡"]
  }
}
```

## 🔍 Поддерживаемые платформы

- **linkedin** — профессиональная сеть, формальный стиль
- **instagram** — визуальная сеть, эмоциональный стиль
- **facebook** — универсальная сеть, смешанный стиль
- **telegram** — мессенджер, свободный стиль

## ✅ Требования к данным

- Минимум 10 авторов
- 10–60 постов на автора
- Минимум 2 платформы на автора
- Длина поста: 20–10000 символов
- Тексты очищены от HTML, нормализованы
- Сохранены эмодзи, хэштеги, структура

## 🚫 Ограничения

Датасет не содержит:
- Токсичный контент
- Политические темы
- Персональные данные
- Рекламу
- Дубликаты постов

## 🔧 Валидация

Для валидации датасета используйте `schema.json`:

```bash
# Пример валидации с помощью Python
python -m jsonschema dataset.json schema.json
```

## 📖 Дополнительная документация

- [Техническое задание](./DATASET_SPEC.md) — полное описание требований
- [JSON Schema](./schema.json) — схема валидации
- [Примеры](./examples/) — примеры данных

## 🔄 Обновление датасета

При добавлении новых данных:

1. Проверьте соответствие схеме (`schema.json`)
2. Убедитесь, что `author_id` уникальны
3. Обновите `version` и `generated_at`
4. Запустите валидацию
5. Обновите эту документацию при необходимости

## 📊 Статистика

[Здесь будет автоматически генерируемая статистика датасета]

- Количество авторов: `N`
- Общее количество постов: `M`
- Распределение по платформам: `{...}`
- Средняя длина поста: `X` символов

