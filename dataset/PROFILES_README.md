# 📊 Author Profiles — Стилевые профили авторов

Стилевые профили авторов, сгенерированные Style Profiler для GhostPen.

## 📁 Формат данных

Профили хранятся в `author_profiles.json` со следующей структурой:

```json
{
  "version": "1.0",
  "generated_at": "2025-02-12T10:00:00Z",
  "profiles": [
    {
      "author_id": "person_01",
      "generated_at": "2025-02-12T10:00:00Z",
      "total_posts": 5,
      "platforms": ["linkedin", "facebook"],
      "style": {
        "avg_post_length": 300,
        "avg_sentence_length": 10.18,
        "tone": {
          "formal": 13.39,
          "emotional": 4.46,
          "expert": 4.46,
          "casual": 0.0,
          "dominant": "formal"
        },
        "emotionality": 2.23,
        "emoji_density": 0.2,
        "hashtag_density": 2.2,
        "structure_type": "paragraphs"
      },
      "platform_specific": {...},
      "topics": {...},
      "signature_phrases": [...],
      "sample_posts": [...]
    }
  ]
}
```

## 🔍 Что анализирует Style Profiler

### Общий стиль (`style`)

- **Длина постов**: средняя, минимальная, максимальная
- **Длина предложений**: среднее количество слов
- **Структура**: использование списков, абзацев, тип структуры
- **Эмодзи и хэштеги**: плотность использования
- **Тон**: формальный / эмоциональный / экспертный / разговорный
- **Эмоциональность**: уровень эмоциональной окраски текста

### Платформо-специфичный стиль (`platform_specific`)

Анализ стиля отдельно для каждой платформы:
- LinkedIn
- Instagram
- Facebook
- Telegram

### Тематика (`topics`)

Определение основных тем постов:
- `career` — карьера, работа
- `motivation` — мотивация, цели
- `personal` — личный опыт
- `expertise` — экспертиза, технологии
- `business` — бизнес, стартапы

### Характерные фразы (`signature_phrases`)

Часто используемые фразы автора (биграммы и триграммы).

### Примеры постов (`sample_posts`)

Примеры постов автора для использования в промптах.

## 🚀 Генерация профилей

Для генерации профилей из датасета:

```bash
python scripts/style_profiler.py dataset/dataset.json dataset/author_profiles.json
```

## ✅ Валидация

Профили можно валидировать по схеме:

```bash
python -m jsonschema dataset/author_profiles.json scripts/author_profiles_schema.json
```

## 📖 Использование

Профили используются в GhostPen для:

1. **Построения промптов** — стилевые характеристики включаются в промпт для LLM
2. **Адаптации под платформу** — используются `platform_specific` данные
3. **Выбора примеров** — `sample_posts` используются как few-shot примеры
4. **Контроля стиля** — метрики используются для post-processing

## 🔄 Обновление

При обновлении датасета:

1. Обновите `dataset.json`
2. Перегенерируйте профили:
   ```bash
   python scripts/style_profiler.py dataset/dataset.json dataset/author_profiles.json
   ```
3. Проверьте валидность

