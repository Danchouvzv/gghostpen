# 🔑 Настройка OpenAI API

## ✅ API ключ установлен

OpenAI API ключ настроен и backend перезапущен с реальной генерацией.

## 🚀 Что изменилось

1. ✅ **OpenAI API ключ** установлен
2. ✅ **Backend перезапущен** с реальной генерацией
3. ✅ **Mock генератор отключен** (используется только при ошибках)

## 📊 Как проверить

### Тест генерации:

```bash
curl -X POST http://localhost:8000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "author_id": "person_01",
    "social_network": "linkedin",
    "topic": "О важности командной работы"
  }'
```

Должен вернуться **реально сгенерированный пост** через OpenAI.

## 🔄 Если нужно перезапустить

```bash
cd /Users/danialtalgatov/Documents/ghostpen/api
export OPENAI_API_KEY="sk-proj-..."
python3 main.py
```

## ⚠️ Важно

API ключ установлен в текущей сессии. Для постоянного использования:

1. Создайте `.env` файл:
   ```bash
   echo 'OPENAI_API_KEY=sk-proj-...' > .env
   ```

2. Или добавьте в `~/.zshrc`:
   ```bash
   export OPENAI_API_KEY="sk-proj-..."
   ```

## ✅ Готово!

Теперь GhostPen использует **реальную генерацию через OpenAI** вместо mock!

